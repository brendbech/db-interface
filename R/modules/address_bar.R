address_bar_UI <- function(id) {
    ns <- NS(id)
    tagList(
    
    )
  }

address_bar <- function(id){
  moduleServer(
    id,
    function(input, output, session){
      
    } #function(input, output, session)
  ) # moduleServer
} #address_bar
